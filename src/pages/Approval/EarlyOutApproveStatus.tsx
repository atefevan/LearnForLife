import InnerLayer from "../../components/InnerLayer";

const EarlyOutApproveStatusPage = () => {
  return (
    <InnerLayer>
      <div>EarlyOutApproveStatusPage</div>
    </InnerLayer>
  );
};

export default EarlyOutApproveStatusPage;
