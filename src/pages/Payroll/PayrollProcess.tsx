import InnerLayer from "../../components/InnerLayer";

const PayrollProcessPage = () => {
  return (
    <InnerLayer>
      <div>PayrollProcessPage</div>
    </InnerLayer>
  );
};

export default PayrollProcessPage;
