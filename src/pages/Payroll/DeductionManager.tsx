import InnerLayer from "../../components/InnerLayer";

const DeductionManagerPage = () => {
  return (
    <InnerLayer>
      <div>DeductionManagerPage</div>
    </InnerLayer>
  );
};

export default DeductionManagerPage;
