import InnerLayer from "../../components/InnerLayer";

const PaymentTermsPage = () => {
  return (
    <InnerLayer>
      <div>PaymentTermsPage</div>
    </InnerLayer>
  );
};

export default PaymentTermsPage;
