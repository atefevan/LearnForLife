import InnerLayer from "../../components/InnerLayer";

const DesignationPage = () => {
  return (
    <InnerLayer>
      <div>DesignationPage</div>
    </InnerLayer>
  );
};

export default DesignationPage;
