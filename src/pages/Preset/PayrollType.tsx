import InnerLayer from "../../components/InnerLayer";

const PayrollTypePage = () => {
  return (
    <InnerLayer>
      <div>PayrollTypePage</div>
    </InnerLayer>
  );
};

export default PayrollTypePage;
