import InnerLayer from "../../components/InnerLayer";

const InstitutePage = () => {
  return (
    <InnerLayer>
      <div>InstitutePage</div>
    </InnerLayer>
  );
};

export default InstitutePage;
