import InnerLayer from "../../components/InnerLayer";

const BankAccountPage = () => {
  return (
    <InnerLayer>
      <div>BankAccountPage</div>
    </InnerLayer>
  );
};

export default BankAccountPage;
