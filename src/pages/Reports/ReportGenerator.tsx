import InnerLayer from "../../components/InnerLayer";

const ReportGeneratorPage = () => {
  return (
    <InnerLayer>
      <div>ReportGeneratorPage</div>
    </InnerLayer>
  );
};

export default ReportGeneratorPage;
