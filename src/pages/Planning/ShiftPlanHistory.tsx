import InnerLayer from "../../components/InnerLayer";

const ShiftPlanHistoryPage = () => {
  return (
    <InnerLayer>
      <div>ShiftPlanHistoryPage</div>
    </InnerLayer>
  );
};

export default ShiftPlanHistoryPage;
